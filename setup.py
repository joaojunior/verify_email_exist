from distutils.core import setup

setup(name='CheckEmail',
      version='1.0',
      description='This service verify if an e-mail is valid and exist',
      author='Joao Junior',
      packages=['api'],)
